# Backend — Authentication, Roles & User Management
# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
alembic==1.14.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
python-multipart==0.0.20

# Encrypted-at-rest SQLite (SQLCipher) is required for the packaged deployment
# and is installed separately (see requirements-encryption.txt). The app falls
# back to a plain local SQLite file for development/testing when it is absent.

# Testing
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1

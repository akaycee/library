"""initial baseline schema

Establishes the full v1 schema from the SQLAlchemy models as the migration
baseline. Subsequent schema changes get their own autogenerated revisions
(``alembic revision --autogenerate``), giving reversible upgrades from here on.

Revision ID: 0001_initial
Revises:
Create Date: 2026-07-21
"""
from __future__ import annotations

from alembic import op

from src.models import Base

revision = "0001_initial"
down_revision = None
branch_labels = None
depends_on = None


def upgrade() -> None:
    Base.metadata.create_all(bind=op.get_bind())


def downgrade() -> None:
    Base.metadata.drop_all(bind=op.get_bind())
